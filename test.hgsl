const V2 = struct{
	member x, y : f32
}
const ret = fn(a) V2{
	return a
}

const main = entrypoint(.fragment){
	uniform ubo : @Buffer(struct{
		member eye_pos: vec3
	})

	const test = ret(.{.x = 1, .y = 2}).y

}
