const Texture = @Texture(f32, ._2d, true)
const doshet = fn(vec : anytype) @TypeOf(vec){
	return vec
}

const main = entrypoint(.fragment){
	uniform tex: Texture

	//const sample = tex.sample(.{0})
	//args:
	//1. vecX - where X is dimensions(3 for cube - direction)
	//2. if(array) u32 - array index else *nothing*
	//rtype:
	//@Vector(X, T) - X - dim, T argument to the @Texture


}
