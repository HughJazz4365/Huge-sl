entry_point.push_constants =>
	root_source_file.push_constants, + 
	entry_point_declared.push_constants

after foldGlobalScope()

construct: entry_points,
		   global_push_constants,
		   entry_points_aliases??, [lists]:

//same as IR.EntryPoint basically
//but everything must be checked by parser before lowering

struct EntryPointInfo{
	name: Token,
    function: Function,
	local_push_constants: List(PushConstantInfo)

	execution_model : ExecutionModel,
	compute_workgroup_size : [3]u32,
}

struct PushConstantInfo{
	type: Type,
	name: Token,
}

entry_point_infos: List(EntryPointInfo),
global_push_constants: List(PushConstantInfo),
